import { theme } from "@/theme.css";
import { style } from "@vanilla-extract/css";

export const zodiacClass = style({});

export const iconClass = style({
  display: "block",
  margin: "0 auto",
  fontSize: theme.fontSize.grand
});
